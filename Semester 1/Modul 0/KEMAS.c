#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a, b, c, d, z;  
    scanf("%d %d %d %d", &a, &b, &c, &d);
    z = a*d-b*c;
    printf("%d", z);
    return 0;
}
